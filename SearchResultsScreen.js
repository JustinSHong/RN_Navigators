import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SearchResultsScreen = ({ navigation }) => {
    return (
        <View>
            <Text>This is the search results screen</Text>
        </View>
    );
};

export default SearchResultsScreen;
