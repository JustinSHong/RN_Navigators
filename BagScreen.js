import React from "react";
import { StyleSheet, Text, View } from "react-native";

const BagScreen = ({ navigation }) => {
    return (
        <View>
            <Text>This is the bag screen</Text>
        </View>
    );
};

export default BagScreen;
