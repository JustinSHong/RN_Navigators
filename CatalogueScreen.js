import React from "react";
import { StyleSheet, Text, View } from "react-native";

const CatalogueScreen = ({ navigation }) => {
    return (
        <View>
            <Text>This is the catalogue screen</Text>
        </View>
    );
};

export default CatalogueScreen;
